import { CreditCardFormatPipePipe } from './credit-card-format-pipe.pipe';

describe('CreditCardFormatPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CreditCardFormatPipePipe();
    expect(pipe).toBeTruthy();
  });
});
